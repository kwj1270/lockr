package com.official.lockr.global.vo;

public enum Position {
    // 공격수 (FW) - 상단
    LW, LF, ST, CF, RF, RW,
    LM, LAM, CAM, RAM, RM,
    LCM, CM, RCM,
    LDM, CDM, RDM,
    LWB, LB, CB, LCB, RCB, RWB, RB,
    GK;

    Position() {
    }
}

